// components/layout/Navbar.tsx
// Fixed top navigation bar rendered on every page via app/layout.tsx.
// Links: Home (/), Diagram (/diagram), Test Yourself (/exercise).
// Use usePathname() from 'next/navigation' to highlight the active link.

export default function Navbar() {
  return <nav></nav>
}
